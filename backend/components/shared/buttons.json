{
  "collectionName": "components_shared_buttons",
  "info": {
    "name": "buttons",
    "icon": "hand-point-up",
    "description": ""
  },
  "options": {},
  "attributes": {
    "colour": {
      "type": "enumeration",
      "enum": [
        "primary",
        "secondary",
        "tertiary",
        "light",
        "dark"
      ],
      "default": "primary",
      "required": true
    },
    "style": {
      "type": "enumeration",
      "enum": [
        "filled",
        "outlined"
      ],
      "default": "filled"
    },
    "direction": {
      "type": "enumeration",
      "enum": [
        "horizontal",
        "vertical"
      ],
      "required": true,
      "default": "horizontal"
    },
    "links": {
      "type": "component",
      "repeatable": true,
      "component": "shared.link",
      "required": true,
      "min": 1
    }
  }
}
